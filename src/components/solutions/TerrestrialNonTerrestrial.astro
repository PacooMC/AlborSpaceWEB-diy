---
import { ArrowRight } from 'lucide-astro';
import NTNDiagram from '../diagrams/NTNDiagram.astro';
---

<div class="glass-card mb-20 overflow-hidden reveal-element">
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
    <div class="p-8 lg:p-10 flex flex-col justify-center">
      {/* Keep green for title/icon */}
      <div class="inline-block mb-4 px-3 py-1 rounded-full bg-green-500/10 text-green-400 text-xs font-medium tracking-widest uppercase">
        5G Integration
      </div>
      <h3 class="text-2xl md:text-3xl font-light mb-4">5G Terrestrial Non-Terrestrial Network Integration Solutions</h3>
      <p class="text-white/70 mb-6 leading-relaxed">
        Our solutions enable seamless service orchestration between terrestrial and satellite networks, creating a unified communications infrastructure for consistent service delivery.
      </p>

      <!-- Key Features List - Keep green for bullets -->
      <ul class="space-y-3 mb-8">
        <li class="flex items-start">
          <span class="w-1.5 h-1.5 rounded-full bg-green-400 mt-2 mr-3"></span>
          <span class="text-white/70">Unified service orchestration across terrestrial and space segments</span>
        </li>
        <li class="flex items-start">
          <span class="w-1.5 h-1.5 rounded-full bg-green-400 mt-2 mr-3"></span>
          <span class="text-white/70">Seamless handover between satellite and cellular networks</span>
        </li>
        <li class="flex items-start">
          <span class="w-1.5 h-1.5 rounded-full bg-green-400 mt-2 mr-3"></span>
          <span class="text-white/70">Dynamic resource allocation for optimal performance</span>
        </li>
        <li class="flex items-start">
          <span class="w-1.5 h-1.5 rounded-full bg-green-400 mt-2 mr-3"></span>
          <span class="text-white/70">3GPP-compliant protocols for universal compatibility</span>
        </li>
      </ul>

      <div class="mt-auto">
        {/* Updated to use minimalist button style */}
        <a href="/solutions/networks" class="primary-button-minimalist group">
          <span>Learn More</span>
          <ArrowRight class="w-4 h-4 ml-2 transition-transform duration-300 transform group-hover:translate-x-1 group-hover:text-[#FF6B00]/80" />
        </a>
      </div>
    </div>

    <!-- 5G NTN Visualization -->
    <NTNDiagram />
  </div>
</div>

<style>
  .glass-card {
    background-color: rgba(13, 15, 26, 0.2); /* dark-800 base */
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 0.75rem;
    transition: transform 0.25s ease, box-shadow 0.25s ease;
  }

  .glass-card:hover {
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
  }
</style>